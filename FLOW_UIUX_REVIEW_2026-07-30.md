# CompassAlpha — 全流程 UI/UX 走查报告

**日期**：2026-07-30
**方法**：本地起服务（PG/Redis/MinIO + API + Vite），375×812 移动视口（Telegram Mini App 的真实尺寸），
按 `下单 → 审核 → 采购 → 验收 → 管理` 逐步操作，每一步截图 + 读 DOM/无障碍树 + 对照源码。
**范围**：交互与呈现为主；走查中撞到的功能缺陷一并记录（都能在代码或 DB 里复现）。

> **实施状态（同日更新）**：第 8 节「先做」+「本地化补齐」两组已全部落地，
> 之后又做完了「定位与去向」一组（第 13、14 条 + 运行身份 + 崩溃屏）。
> 逐条见 [§10 已实施](#10-已实施2026-07-30) 和 [§11 定位与去向](#11-定位与去向2026-07-30)。
> 走查结论中有 **两条我判断错了**，已在 §1.9 和 §4.3 原地更正 —— 别按初版结论动手。
> 第三轮撞到一个根因、六处误判，其中**两个已上线的管理功能完全不可用** ——
> 见 [§12](#12-一个根因六处误判2026-07-30第三轮)。
> 第四轮把 §13 那两个开放决策定了并实现：舍入策略实测后**关闭为非缺陷**，
> 多批次**露出来** —— 见 [§14](#14-两个开放决策我定了2026-07-30第四轮)。
> **现在没有待你拍板的项**。第五轮做完了 §14.5 的前三件（历史折叠、支付方式统一、
> 审核卡片去重）—— 见 [§15](#15-1445-的前三件2026-07-30第五轮)。剩两件：77 处深层 i18n、审批人补录 SKU。
>
> 走查结论里 **三条被我自己更正过**：§1.9（量级错了一个数量级）、§4.3（诊断方向错了）、
> §1.2（21 处实际是 124 处）。别按初版结论动手。

---

## 0. 本地运行记录

```bash
docker compose -f infra/compose/docker-compose.dev.yml up -d
pnpm install && pnpm db:migrate && pnpm db:seed
pnpm dev:api                      # :3000
cd apps/web && bun --bun x vite   # :5173
```

两点与 README 不符，值得回写文档：

- **本机没有 Node.js，只有 Bun**。`pnpm dev:web`（`vite`）和 `pnpm db:generate`（`drizzle-kit`）都因
  `'node' is not recognized` 失败。`bun --bun x vite` 可以正常跑起来。`db:generate` 只在改 schema 时需要，
  已有 migration 时可跳过。
- `pnpm db:seed` 后 DB 里已有 343 个门店，其中绝大多数是 E2E 测试残留（`A-0.388…`、`SalesB2-0.85…`、
  `Renamed A 1782832020830` 等）。测试没有清理自己的数据，本地库很快就没法用来做真实演示了。

---

## 1. 贯穿全流程的问题（优先修这些，一次修好五个页面）

### 1.1 单位标签一半中文一半英文 —— 同一个概念两种写法

`useUnitLabel()` 把 `kg → 公斤`、`pcs → 个` 本地化，但**只有一半页面用了它**：

| 页面          | 显示          | 代码                                      |
| ------------- | ------------- | ----------------------------------------- |
| 下单 商品行   | `0.5 kg` ❌   | `OrderPage.tsx:981` `unit={sku.unit}`     |
| 下单 复核弹窗 | `0.5 kg` ❌   | `OrderPage.tsx:1131` `{r.unit}`           |
| 验收 商品行   | `kg` ❌       | `ConfirmPage.tsx:312` `{sku?.unit ?? ''}` |
| 审核          | `0.5 公斤` ✅ | `ApprovalPage.tsx:656`                    |
| 采购          | `21 公斤` ✅  | `RunViews.tsx:455`、`RunPanels.tsx:564`   |

`RunViews.tsx:70` 的注释里已经写了「不要在两个屏幕上用两种方式显示同一个东西」——但最主要的那个屏幕
（下单，员工一天用几十次）恰好是漏掉的那个。**下单页是全 App 单位显示唯一还是英文的地方。**

### 1.2 硬编码英文串 —— CI 的 i18n 检查抓不到

README 写「i18n 不打折，CI grep 检查 zh/en/ru/uz 全键存在」。问题是 CI 只能校验**已经成为 key 的串**，
对从来没进 catalog 的字面量无能为力。

> **数量更正**：初版说「21 处」。后来写了检测脚本（`scripts/check-untranslated.ts`）跑全量，
> 实际是 **124 处**。我手数只覆盖了按钮和确认框，漏掉了大量空态文案、sheet 标题、aria-label，
> 以及 `@compass/ui` 自己渲染的全部字符串。本轮清掉 45 处，剩 79 处已被 ratchet 卡住（见 §10）。

手数阶段确认的（已全部修复）：

**管理页 15 个按钮 + 6 个弹窗标题/确认框**

```
CatalogSections.tsx:155,477,765        Edit
CatalogSections.tsx:168,489,777        Archive
CatalogSections.tsx:497,785            Unarchive
CatalogSections.tsx:388                + New SKU
CatalogSections.tsx:182,512,798        'Edit category' / 'New SKU' / 'Edit supplier'
CatalogSections.tsx:484                nativeConfirm(`Archive SKU "${primary}"?`)
CatalogSections.tsx:772                nativeConfirm(`Archive supplier "${sp.name}"?`)
StoresSections.tsx:230,1071,1088,1093  "New store" / Save / `Archive "${store.name}"?` / Archive
PeopleSections.tsx:819,1037,1046,1054  "New role" / Save / Edit / `Delete role "…"?`
OperationsSections.tsx:490,545          Delete
```

最能说明问题的一点：**同一个文件里**，`CatalogSections.tsx` 的菜品段（1435、1470 行）规规矩矩用
`i18n.t('common.archive')`（zh 已有译文「归档」），而它上面的分类/商品/供应商三段全是硬编码 `Archive`。

**数量快捷选择弹窗**（`QtyControl.tsx`）整个是英文：`Set quantity`(340)、`Unit: kg`(341)、
`Custom`(396)、按钮 `Set 0.5 kg`(319-322)，以及无障碍标签 `Decrement`(175) / `Increment`(222) /
`Quantity 0.5 kg — tap to pick`(199)。这是下单页的核心交互。

### 1.3 日期没走本地化

`toLocaleDateString(undefined, …)` 用的是浏览器 locale，不是 App locale：

- 下单复核弹窗副标题：`Thursday, July 30 · 1 件商品`（`OrderPage.tsx:625`）
- 采购历史月份头：`JUNE 2026`

中文界面里冒出英文月份。同时，所有日期都是裸 ISO（`2026-06-25`），没有「今天 / 昨天 / 6月25日」，
和已经本地化的单位标签是两套语言策略。

### 1.4 返回和设置整个外包给了 Telegram —— 已验证的死胡同

- **返回**：管理页的二级/三级页面只接 Telegram 原生 BackButton（`AdminPage.tsx:419`），页面内没有任何
  返回控件。`useTelegram.ts:156` 的注释写得很明确：「in-page chevron buttons can be removed once this is wired」。
- **设置**（语言、门店切换、个人资料）只挂在 Telegram 的 `⋯` → 齿轮（`Shell.tsx:164`）。

实测后果（浏览器里，也就是 `.env.example` 里写的 `NON_TELEGRAM_LOGIN` 诊断路径）：

1. 进 `管理 → 组织`，**没有任何返回入口**。
2. 再点已激活的「管理」tab —— 不回一级（点当前 tab 回根是移动端通用约定）。
3. `adminSection: "organization"` 被持久化进 `localStorage['compass.nav.v1']`，**刷新页面也回不去**，
   只能手改 localStorage。

即使在 Telegram 里也有代价：门店切换器藏在 `⋯` 溢出菜单的齿轮里（两跳、默认不可见），而它决定了你在给
哪个门店下单 —— 配合下一条，风险不小。

### 1.5 当前门店在任何页面都不显示

下单、审核、验收都强依赖「当前门店」，但**五个页面没有一处显示门店名**。我是在审核页看到卡片上写着
`Eden Magic City` 才知道自己在给哪家店下单的。

`OrderPage.tsx:633-642` 的注释解释了为什么删掉了 `[日期 · 已选数]` 那一行：
「Date appears in the Telegram chrome (bot title bar shows the current view)」——**这个前提是错的**。
Telegram 的 chrome 显示的是 bot 名字，不是 App 的当前视图，也不是日期。所以是基于一个不成立的理由把
定位信息删掉了。

组合起来：门店切换藏两层 + 当前门店永不显示 + 切换结果持久化到 localStorage
= 管理员上周切到 Eden Seoul，今天全天给 Eden Seoul 下单而毫无察觉。

**建议**：粘性条最左侧放一枚门店 chip（`Eden Magic City ▾`），既是当前上下文、又是切换入口。
占一行文字的宽度，解决整条链路上最危险的一个歧义。

### 1.6 两套主按钮系统在打架

`Shell.tsx:362-370` 明确弃用了 Telegram 原生 MainButton，改成 DOM 内的 `<PageMainButton />`
（原因：原生按钮在 WebView 下方，会把底部导航顶上去），并且**永久 `tg.MainButton.hide()`**（474-478 行）。

但 `QtyControl.tsx:327` 的快捷选择弹窗仍然去抢原生 MainButton，而且判定条件是
`!!getMainButton()` —— `index.html` 里静态引了 `telegram-web-app.js`，所以**这个条件永远为真**。后果：

- 真机 Telegram 里：打开数量弹窗会把已经退役的原生按钮召回来，底部导航被顶起 —— 正是 M3.49 要消除的抖动。
- 浏览器里：`hasTelegramMainButton === true` → 弹窗内的兜底 `Set` 按钮被跳过（`QtyControl.tsx:348`），
  而原生按钮不存在。**「Custom」输入框填了值没有任何按钮可以确认**（已在无障碍树里确认：弹窗内只有
  预设 chip 和一个 number 输入框，没有确认、也没有关闭）。

### 1.7 同一个选择，两种控件

`PaymentMethodChips.tsx` 的注释自己写了：

> NOTE: PurchaseRow's inline cash/transfer control is a DIFFERENT affordance (a single icon-only
> toggle …) and is intentionally not folded in here. … belongs to the Phase 6 emoji sweep.

- 弹窗里：`💵 现金` / `🏦 转账` 两枚带文字的 chip —— 清楚。
- 采购行内联编辑器里：一个光秃秃的 `💵`，`aria-label="支付方式: 现金"`，说明只在 `title` 里。
- 采购历史汇总行里：`💵 6,066,500 · 🏦 1,305,000` —— 纯 emoji 当图例。

「Phase 6 emoji sweep」一直没做。现金/转账直接决定结算口径，不该靠一个 emoji 表达。

### 1.8 只有 `title` 的图标控件 —— 触屏上永远看不到

移动端没有 hover，`title` 属性等于不存在。目前靠 `title` 承载全部语义的控件：

| 控件     | 屏上显示   | 真实含义（只在 title 里）              |
| -------- | ---------- | -------------------------------------- |
| `×千`    | 两个字     | `按千位输入价格 (×1000)`，**默认开启** |
| `+ 新增` | `+ 新增`   | `采购中新增物品`                       |
| `💵`     | 一个 emoji | `支付方式: 现金`                       |

`×千` 最危险：默认 `aria-pressed="true"`，把采购员输入的每一个价格乘以 1000。开/关两态只有一点底色差别，
没有任何文字说明。虽然行内有 `= 20,000` 的实时回显做兜底，但一个采购员看不出 `20` 为什么等于 20,000。
**建议**：换成显式的两态分段控件 `UZS | 千 UZS`，或者干脆把单位写在输入框后缀里（`20 千 UZS`）。

### 1.9 金额精度 —— ⚠️ 初版判断有误，已更正

**初版写的**：`.9` 是浮点残留，建议 formatMeny 按币种取整。**这个结论是错的，不要照做。**

查下去之后：

1. `formatMoney`（`lib/format.ts`）遵循的是一条**已经记录在案的用户规则** ——
   「钱数量金额需要用 xxx,xxx,xxx 这种格式，小数点否保留一位就可以」。最多一位小数是有意为之。
2. `.9` **不是浮点残留，是真实数据**。库里有一条 `unit_price = 23333.30`、`qty = 3.000`，
   行小计 69,999.90 —— 也就是「70,000 买了 3 份」被除出来的单价。

```sql
select unit_price, qty, unit_price*qty from inventory.price_history
where unit_price <> round(unit_price);
--  23333.30 | 3.000 | 69999.90000
```

所以显示层是忠实的，**没有改动 formatMoney**。

**第二处更正（同日，第五轮）：初版还写了「『总价 ÷ 数量』会丢钱，需要你定舍入策略」。
这一条也是错的 —— 代码里根本没有除法。**

`packages/domain/src/run/events.ts:291` 把契约写得很清楚：

> `unitPrice * qty = total`. For lump-sums qty=1 and unitPrice=total.

也就是说采购员录的是**单价**；遇到一次性总价就 qty=1、单价=总价。全仓 grep
「总价除以数量」这类表达式，**零命中**。`settlement.ts` 的每一步都是
`Number(split.qty) * Number(unitPrice)` 再求和，同样没有除法。

那条 `23333.30` 是一条 `finished` 采购单里**有人手填的单价**（或测试数据），
`3 × 23333.30 = 69,999.90` 是精确乘法，一分没丢。

跨全部真实数据验证过分账不变量：

```sql
-- 每条已购行：分到各门店的数量之和 是否等于 该行采购数量
purchased_lines | with_splits | qty_mismatch | max_qty_residual
           1740 |        1740 |            0 |            0.000
```

**1,740 条已购行，全部有门店分账，零不匹配，最大残差 0.000。**
分账切的是**数量**（`evenSplitQty` / `computeProportionalSplits`），金额始终是
数量 × 单价的精确乘法，所以没有可积累的舍入损失。

**结论：没有舍入策略需要决定，也没有东西要改。** 这一条从待决策项里彻底移除。

我在这份报告里对同一个 `.9` 连错两次 —— 先说是浮点残留，纠正后又说是除法丢钱。
两次都是从一个数字往回推测机制，而没有先去读那条数据是怎么产生的。第二次纠正
本该在第一次就做完。

---

## 2. 第一步：下单

### 2.1 数量为 0 时中间那个按钮**什么都不渲染** 🔴

`QtyControl.tsx:210` —— `{showZero ? '' : (…)}`。

一张新订单上 244 行，每行都是 `−` 和 `+` 之间一段 64px 宽的**空白**。那段空白其实是个按钮
（`aria-label="Quantity 0 kg — tap to pick"`），点开就是预设 chip 快捷选择 —— 也就是「一次点到 12 公斤」
的快路径。但它完全不可见、不可发现。同时这段空隙让每一行看起来像是渲染坏了。

**建议**：0 时显示占位符（一个淡色 `0`，或者直接把单位画上去 —— `kg`），把 tap 目标暴露出来。
顺手把 1.1 的本地化一起改掉。

### 2.2 244 行、12,138px、无虚拟化，「其他物品」埋在最底下

实测：`main.scrollHeight = 12138`，视口 611px，DOM 里 244 个 `<li>`，无虚拟滚动。

- 「其他物品」（目录里没有的东西）在整个目录之后，要滚约 **20 屏**才能到。
  `OrderPage.tsx:784-793` 的注释说这是「natural placement」，实际上等于藏起来了。
- 没有回到顶部、没有分类吸顶头、没有 A–Z 索引。
- 分类 chip 有 13 个，375px 下只看得见 2.5 个，且没有任何可横滑的提示（没有渐隐、没有箭头）。
- 搜索框 placeholder 被截断：`搜索商品（任何语言` —— 右括号和省略号丢了。

**建议**：

1. 「+ 其他物品」提到粘性条里，或做成一个常驻浮动按钮。
2. 分类 chip 右侧加渐隐遮罩；或改成一个「分类 ▾」下拉，把 13 个选项一次列全。
3. 列表加虚拟化 + 分类吸顶头。
4. placeholder 缩短成「搜索商品…」。

跨语言搜索本身很好用 —— 输 `pomidor`（乌兹别克语）能搜出 `西红柿`、`番茄`。**但结果只显示中文名，
不告诉你是哪个别名命中的**。建议把命中的那个语言名作为第二行灰字显示，用户才敢确认自己找对了。

### 2.3 提交之后看不到自己提交了什么 🔴

提交后：横幅「已提交 · 等待审核」+「撤回」，然后是**244 行灰掉的目录**。

- 已提交的内容没有任何汇总。想知道自己报了什么，只能在 244 行里翻找那些非零的灰字行。
- 复核弹窗也回不去了 —— 主按钮的显示条件是 `visible: canSubmit && selectedCount > 0`
  （`OrderPage.tsx:592`），提交后 `canSubmit` 为 false，按钮消失。**没有任何路径能重看已提交的订单。**

**建议**：提交后把整页换成一张只读小票（门店 · 日期 · 分组明细 · 预估金额 + 撤回），而不是留一整页
灰掉的目录。

### 2.4 后端支持一天多批，前端完全没体现

`todaySession`（`order.ts:112-144`）优先返回未提交的 draft，否则返回一条**未被认领的**已提交单。
所以：

- 一旦经理认领了你的订单，它就从你的下单页彻底消失，连横幅都没有。
- 只要你新开了一批，之前提交的那批就被覆盖，看不见了。

`order.ts:118-120` 的注释说「returns null and the FE shows an empty-state with a
'start a new batch' affordance」—— **这个空态和「新建一批」入口在前端并不存在**，页面就是渲染一遍全零目录。

实测：我提交 0.5kg 洋葱 → 通过审核 → 回到下单页，看到的是 `洋葱 1.5 kg` 的**可编辑草稿**，
而且没有任何提示告诉我刚才那单已经通过了。（1.5 那单是怎么来的见 §3.4。）

**建议**：粘性条下加一条「今日已提交 2 批 ›」，点开是批次列表 + 各批状态；「开新一批」做成显式动作。

### 2.5 复核弹窗

- 弹窗里**改不了数量**，看到错的得关掉、在 244 行里找到那一行、改、再打开。
- **没有门店名** —— 提交前最后一屏不告诉你在给哪家店提交。
- 日期是英文（§1.3）。
- 预估金额那块设计不错（avg-7d + 「N 项有历史 / M 项没有」），但本次没有价格历史所以整块不渲染，
  没有「暂无价格历史，无法预估」的兜底文案。

### 2.6 数量快捷弹窗

除了 §1.2 的英文和 §1.6 的无确认按钮：

- 没有关闭/取消按钮，只能点外部区域关 —— 不可发现。
- **Custom 输入会静默吸附到 step**（`QtyControl.tsx:314` `roundToStep`），而 `+/−` 按钮
  （`adjust`，101-102 行）在 M3.35 时**特意改成不吸附**（注释：「Off-step values stay off-step —
  the data is the data」）。同一个弹窗里两条相反的规则：输入 0.55（step 0.1）变成 0.6，点 `+` 不会。

---

## 3. 第二步：审核

### 3.1 卡片上没有任何可以据以判断的信息

卡片只有：门店名、`由 Nurzat 提交 · 2026-07-30`、`认领` / `查看明细`。
**没有商品数、没有预估金额。** 审批人要判断这是 1 项还是 80 项，只能把每张卡都展开一遍。

`ApprovalPage.tsx:231-236` 记录了这是主动删掉的：

> dropped the "{itemCount} · {totalQty}" Badge — the View items expansion + the inline estimate row
> already surface count + total

这个理由是倒过来的：**分诊需要的信息必须在展开之前就能看到**。删掉之后每张卡都强制展开一次。
这是「declutter 过头」的典型。

**建议**：卡片右侧恢复 `12 项 · ~450K`。这一条就把审批人的工作量从 N 次展开降到 0 次。

### 3.2 头像和标题说的不是同一个人

`ApprovalPage.tsx:204-208`：`src={row.attribAvatarUrl}`（提交人的 Telegram 头像）

- `name={row.storeName ?? …}`（门店名 → 用作 alt 和首字母兜底）。

实测 DOM：`<img src="https://t.me/i/userpic/…svg" alt="Eden Magic City ">` ——
**一张人脸配一个门店名的 alt**。视觉上它挨着标题「Eden Magic City」，看起来像门店 logo，实际是提交人头像；
读屏软件会把人像念成「Eden Magic City」。

顺带：`alt="Eden Magic City "` 尾部有空格 —— 门店名在 DB 里带空格，展示时没有 trim。

**建议**：确定这个槽位到底代表谁。若代表提交人，就把 `name` 也换成提交人（`attribDisplayName`），
标题继续是门店名；若代表门店，就别用提交人头像。

### 3.3 分段控件在 375px 下被切掉

`待审核 / 已通过 / 已拒绝` —— 第三项被截成「已拒纟」。在最主要的目标宽度上溢出。

### 3.4 认领后：4 个按钮、审批零摩擦、拒绝有摩擦

认领后一行并排 4 个按钮：`通过`(蓝实心) `拒绝`(红实心) `释放`(pearl) `收起`(ghost)。

- **通过 = 一点即生效，无确认、无撤销入口**；**拒绝 = 强制填理由的弹窗**。
  风险方向是反的 —— 通过才是把订单放行到采购、产生实际支出的动作。
  `已通过 ✓` 的 toast 里也没有「撤销」，尽管 `unapprove` 接口是存在的。
- 决策动作（通过/拒绝）和元动作（释放/收起）混在同一行、同一视觉层级。
- 红实心「拒绝」和蓝实心「通过」权重相等。

**建议**：`通过` 单独做主按钮；`拒绝` 降为描边；`释放`/`收起` 挪到次级行或溢出菜单。
`已通过` 的 toast 带一个 5 秒「撤销」。

### 3.5 审批人的 +/− 步长永远是 1 🔴

`ApprovalPage.tsx:135-141` 构造 `skuById` 时**没有放 `step`**：

```ts
m.set(sku.id, { names: sku.names as Record<string, string>, unit: sku.unit });
```

而 655 行读的是 `step={Number(sku.step ?? '1')}`（类型里 `step?: string` 是可选的，所以 TS 不报错）。
结果：`sku.step` 永远 undefined → 步长恒为 1。

同一个 `QtyControl`，下单页按 0.5 走、审核页按 1 走。采购页拿到的是真 step（DOM 里 `step="0.500"`）——
**只有审核页漏了**。

### 3.6 审批人改自己提交的单会静默生成一张幽灵草稿 🔴

**复现**：同一个成员既有 `order.draft` 又有 `order.approve`（super_admin、以及自己给自己店下单的
店长/老板都是这种情况）→ 提交订单 → 认领 → 点商品行的 `+`。

**现象**：HTTP 200，响应 `{"applied":[]}`，界面数字不变，没有 toast，没有报错。

**实际发生的**（DB 已验证）：

```
9b636f4f… | 2026-07-30 | draft     | 洋葱 1.500   ← 新建的幽灵草稿
01bb144f… | 2026-07-30 | submitted | 洋葱 0.500   ← 本该被改的那张，没动
```

原因在 `order.ts:533-535`：

```ts
const isManagerEdit =
  input.targetMemberId !== undefined && input.targetMemberId !== ctx.session!.memberId;
```

当审批人 == 提交人时 `isManagerEdit` 为 false → 走「找我自己的 draft」分支 → 找不到（我那张是
submitted）→ 落到 lazy-create 分支，**新建了一张草稿并把改动写进去**。
`if (input.targetMemberId && input.targetMemberId !== memberId)` 这道保险因为两者相等而没有拦住。

副作用：这张幽灵草稿随后成了下单页显示的那一条（§2.4），所以员工回到下单页看到的是
「洋葱 1.5 kg 草稿」，而真正通过的 0.5kg 那单从视野里消失了。

### 3.7 审批人只能改数量，不能补漏

只能加减/清零，**不能新增员工漏报的 SKU**。发现漏了就只能整单拒绝、让员工重报一遍。
对一个「审核」环节来说，这是个明显的能力缺口。

### 3.8 看不出哪个数字是审批人改过的

审批人在原地改完数量后，界面上没有任何痕迹区分「员工报的」和「审批人改的」。
事件流里有 `prevQty`，完全可以展示成 `0.5 → 1 公斤`（划掉原值）。

### 3.9 队列缺少分诊线索

- 3 天前（2026-07-27）的待审核单没有任何超期标记。
- tab 上没有计数（`待审核 2`）。
- 没有日期分组、没有相对时间。

---

## 4. 第三步：采购（最大的一屏，也是问题最集中的一屏）

整个采购页是**一条 9,377px 的滚动流**，依次是：

| 位置     | 内容                                                |
| -------- | --------------------------------------------------- |
| 0        | 粘性条：`×千` `+ 新增` `#1 · 2026-07-27 · 采购`     |
| 60       | 「有 1 个新审批通过的订单可加入本次采购」+ 加入按钮 |
| 172      | 本次采购的提交（10 份）                             |
| 714      | 按门店 / 按摊位 / 按类型                            |
| 771      | 已买 2 · 缺货 0 · 剩 85                             |
| 849      | 全部 87 / 待办 85 + 搜索框                          |
| 900–7800 | **87 行商品（≈6,900px）**                           |
| 7821     | 其他物品                                            |
| 8046     | 目录外 & 开销                                       |
| 8161     | 历史记录（20+ 个月）                                |

### 4.1 粘性区给了两个最少用的控件，最需要的全滚走了 🔴

常驻可见的是 `×千`（一次运行设一次）、`+ 新增`（罕用）、和一段小灰字的运行编号。
而**搜索框、`待办` 过滤、进度计数（剩 85）在滚进列表的瞬间就全部消失**。

采购员站在市场里要处理 85 项，他每一秒都需要搜索和「还剩多少」。这三个恰好是滚走的那三个。

**建议**：粘性条改成 `[门店/运行标识] [搜索] [待办 85 ▾]`；`×千` 挪进设置或运行头部；进度做成一条
2px 的进度条贴在粘性条底边。

### 4.2 采购中「开始配送」按钮被完全隐藏 🔴

`RunPage.tsx:982-991`：

```ts
case 'purchasing':
  // 2026-05-04: HIDE the button entirely when not actionable.
  // The previous "Process every item first" disabled label ate 60 vertical
  // pixels with zero value — user already knows from the row badges what's pending.
  return { …, visible: allItemsHandled, active: allItemsHandled };
```

于是采购页在 `purchasing` 状态下：底部没有 CTA、没有说明门槛是什么、进度计数还滚走了（§4.1）。
屏幕上没有任何东西回答「我下一步该干什么」。

「用户从行标记就知道还剩什么」这个假设不成立 —— 剩下的 85 项分散在 6,900px 里。

**建议**：不要放死的 disabled 按钮，也不要什么都不放。放一条常驻的进度/下一步条：
`剩 85 项待处理 ›`（点一下跳到第一个未处理项），归零后原地变成 `开始配送`。
同一个位置、同一条信息，从「死胡同」变成「导航」。

### 4.3 分组 chip 的语义错了（不是「缺选中态」）—— ⚠️ 初版判断有误，已更正

**初版写的**：三个 tab 全是 `aria-selected="false"`，缺选中态，建议补上。**这个诊断是错的。**

读了 `RunPanels.tsx:366-375` 的 M3.28 决策记录后才明白：这三个**本来就是可开可关的 toggle**，
「三个都不亮」= 回到可编辑的 aggregate 视图，是刻意设计的默认态。用户当初明确要求「点亮的 chip
再点一下要能取消」。所以不该给它硬塞一个选中项。

真正的问题是**用错了 ARIA 角色**：`Chip` 把 `role="tab"` 写死了，`ChipBar` 把 `role="tablist"`
写死了。一个没有任何选中项的 tablist 是无效的 ARIA（tab 模式要求恰好一个选中），读屏软件会
念成「一组标签，没有选中项」，而它实际是三个独立开关。

**已改**：给 `ChipBar`/`Chip` 加了 `variant`，通过 context 下发，调用方只在容器上声明一次：

| variant        | 角色                                    | 用在哪                                  |
| -------------- | --------------------------------------- | --------------------------------------- |
| `tabs`（默认） | `tablist` / `tab` + `aria-selected`     | 下单页分类、`全部/待办` —— 恒有一个选中 |
| `toggles`      | `group` / 无 role + `aria-pressed`      | 采购页 `按门店/按摊位/按类型`           |
| `radio`        | `radiogroup` / `radio` + `aria-checked` | 验收页状态 chip —— 「还没选」是合法状态 |

`全部 87 / 待办 85` 保持 tablist 不变。这样一个页面里两组 chip 的语义各自正确，而不是被迫统一。

### 4.4 切到分组模式，搜索和过滤直接消失

点「按摊位」后：`商品` 段头、搜索框、`全部/待办` 过滤**整块不见了**。
想搜东西必须先切回不分组。两套互斥的控件集，切换成本没有任何提示。

### 4.5 混合单位求和 —— 一个没有意义的数字，还有两种叫法

- 提交列表：`27 个 SKU · 66.5 个单位`
- 摊位分组头：`19 个 SKU · 139.5`（连单位词都没有）

把 27 公斤洋葱和 5 把刀加起来得到「66.5 个单位」，这个数字没有任何可解释的含义。
而且两处标注方式还不一样。

**建议**：只留 `27 个 SKU`；要给量感就按单位族拆开（`≈45 公斤 · 12 件`），或者换成金额
（`27 个 SKU · ~1.2M`）—— 金额才是采购员真正关心的量纲。

### 4.6 价格是一个月前的，而这个事实只有读屏软件知道 🔴

商品行的估价 DOM：

```html
<span title="20,000 UZS">~20K<span class="sr-only"> 34 天前</span></span>
```

**「34 天前」在 `sr-only` 里 —— 视觉上完全看不到。** 而行尾那枚大蓝 `✓`
（`aria-label="保存采购：大蒜"`）是「按这个估价一键入账」。

也就是说：采购员一点就用一个月前的价格记了账，屏幕上没有任何陈旧提示，也没有确认。
这是全 App 风险最高的一次点击。

**建议**：把新鲜度画出来 —— `~20K · 34天前`，并按阈值变色（7 天内正常、30 天以上警示）。
超过阈值时，`✓` 快捷入账要么禁用要么二次确认。

### 4.7 每一行都是饱和蓝 ✓，看起来像全都做完了

87 行，每行行尾一枚实心蓝 `✓`。实心 + 品牌色在设计语言里通常表示「已完成/已激活」，
所以整页扫下来像是「全都处理过了」，而实际是 85 项未处理。

真正的状态载体是行首那个 **7px 的圆环**，含义只写在 `sr-only`（「未购买」）里，没有图例。

**建议**：把「已购买 / 未购买 / 缺货」做成同一个明确的状态维度（比如整行左侧 3px 色条 +
文字状态 chip），`✓` 只在未购买时出现、且改成描边样式。

### 4.8 「其他物品」用的是另一套行设计

目录商品行：状态点 + 名称 + 数量 + 估价 + `✓` + 门店 chip。
其他物品：`· 名称 …… 2 公斤 [记价]` —— 项目符号列表 + 一个描边小按钮。没有状态点、没有缺货入口。

概念上是同一件事（要买的东西），采购员在市场里要在两种列表语言之间来回切换。
而且从进度计数「已买 2 · 缺货 0 · 剩 85」推断，其他物品大概没被算进去 —— **进度是不完整的**。

### 4.9 20 多个月的历史内联在活动运行页里

`历史记录` 在 8,161px 处，要越过 87 行在办商品才能看到。而它旁边就有一个「全部历史」入口 ——
说明完整历史视图本来就存在，内联这一份是纯冗余。

历史行本身也不好扫：`合计 7,371,500 UZS · 💵 6,066,500 · 🏦 1,305,000` 挤成一行、跨行不对齐，
没法上下比较。没有期间汇总、没有筛选。

同日两次运行显示为 `2026-06-24` 和 `2026-06-24 #2` —— **第一次没有 `#1`**，看不出哪个是哪个。

**建议**：历史折叠成一行摘要（`本月 65.9M ›`）跳到独立视图；独立视图里金额右对齐成一列，
现金/转账用文字标签，加月度小结。

### 4.10 运行身份是屏幕上最不显眼的元素

`#1 · 2026-07-27 · 采购` —— 最小字号、灰色、右对齐。它是这一屏所有内容的定语。
而且这个「活动运行」的日期是 2026-07-27，**已经 3 天了，没有任何提示**。

### 4.11 粘性条压住段头

滚动时粘性条会盖住 `商品` 段头和「已买 2 · 缺货 0 · 剩 85」那一行（截图可见被切一半）。
段落需要 `scroll-margin-top`。

---

## 5. 第四步：验收

本地没有运行走到 `delivering`，队列是空的。空态文案写得很好：

> **暂无可确认** / 当采购员把商品送到你店里时，这里会出现待确认的清单。

以下来自代码审查，两条合起来问题不小。

### 5.1 整个验收环节默认是空转的 🔴

```ts
// ConfirmPage.tsx:177
const allDecided = myItems.length > 0;
```

变量叫 `allDecided`（全部已判定），实际只判断了「至少有一项」。它直接喂给
`mainBtnActive = allDecided && !confirmStore.isPending`（221 行）——
**「确认收货」按钮从一开始就是可点的，不要求收货人对任何一项做过判断。**

```ts
// ConfirmPage.tsx:297
const effectiveStatus = (it.confirmStatus ?? 'ok') as DecisionStatus;
```

**未判定的项默认显示成「ok」。**

两条叠加：收货人打开页面，看到所有商品都已经勾在「ok」上，底部按钮可点，一下点完 ——
整批签收为「收到且无误」，而他一项都没看。而且事后**无法区分「查过没问题」和「根本没看」**。

对一个把「签收对账」写进产品定义的系统，这是核心价值的漏空。

**建议**：

1. `allDecided` 改成名副实：`myItems.every(it => it.confirmStatus != null)`。
2. 未判定项渲染成「未判定」态（灰、无选中），不要预选 ok。
3. 需要快速通道就给一个显式的「全部标记为无误」按钮 —— 一次点击、意图明确、可审计，
   而不是把它伪装成默认状态。

### 5.2 其他

- 单位是裸英文（§1.1）。
- 没有门店名（§1.5）—— 收货人确认的是「我这家店」的货，页面不写是哪家。
- 粘性条只有一行右对齐的灰色运行日期，和采购页一样的弱定位。

---

## 6. 第五步：管理

一级 hub 是**全 App 最好的一屏**：5 行，图标 + 标题 + 说明 + 陈列符，层级清楚、好扫。问题都在细节和下钻上。

### 6.1 二级 hub 丢了一级的图标

`管理` 的 5 行有图标；`目录` 下的 5 行（分类/商品/供应商/菜品/开销模板）**没有图标**。
同一个导航模式，两级两种样式。

### 6.2 副标题的语气不统一

- 组织：`组织信息、语言、时区` ← 内容清单
- 角色与权限：`内置角色、等级、权限矩阵` ← 内容清单
- 目录：`分类、商品、供应商` ← 内容清单
- 运营：`活动、订单历史、维护` ← 内容清单
- **门店：`点击门店管理其人员与设置`** ← 操作指引

五行里四行是「有什么」，一行是「怎么用」。改成 `门店列表、人员、设置`。

### 6.3 组织页

- **币种/税率重复且互相矛盾**：只读表里 `币种 UZS`、`税率 —`；下面「财务设置」行的副标题写
  `UZS · 税 0%`。同一屏、同一个值，`—` 和 `0%` 两种写法。
- `Telegram` 这一行的标签是英文，其余四行（名称/我的角色/币种/税率）都是中文。
- `我的角色 super_admin` —— 直接把枚举 key 给用户看。别处角色都有显示名。

### 6.4 商品列表

- `+ New SKU` / `Edit` / `Archive` 三个英文按钮夹在中文界面里（§1.2）。
- `step 0.500` —— 标签英文，值带三位无意义小数。`步长 0.5` 就够。
- 元信息是 5 个值用 `·` 串起来、无标签：`肉类禽类 · kg · step 0.500 · BEEF` ——
  要靠猜才知道 `BEEF` 是编码。缺分类时显示 `—`，混在 `·` 分隔里像是一个值而不是「空」。
- **每行两个等宽次级按钮 `Edit` / `Archive`**，约 190 个 SKU = 380 个按钮。
  归档（半破坏性）和编辑权重完全相同。行本身其实已经可点（菜品段就是这么做的）。
  **建议**：点行编辑，归档收进侧滑或溢出菜单。

### 6.5 订单历史只在管理里

`运营 → 订单历史` 需要管理权限。普通员工看不到自己报过什么 —— 和 §2.3 / §2.4 是同一个缺口的另一面。

---

## 7. 走查中撞到的一次整页崩溃 ⚠️

从 `采购` 切到 `验收` 时，整个 App 掉进 ErrorBoundary：

> **Something broke before the app could render.**
> Maximum update depth exceeded. …
> at forceStoreRerender / updateStoreInstance / commitHookEffectListMount / commitPassiveMountOnFiber

`forceStoreRerender` + `updateStoreInstance` 是 `useSyncExternalStore` 的重渲染循环 ——
典型原因是 store selector 每次调用都返回新的对象引用（zustand 默认用 `Object.is` 比较）。

**首要嫌疑**：`StoreSwitcher.tsx:120`

```ts
const stores = useAuthStore((s) => s.session?.stores ?? []);
```

`session` 为 null 或 `stores` 为 undefined 时，每次调用都产生一个**新的 `[]`** →
快照永远「变了」→ 无限重渲染。`useStoreContext()` 被下单/审核/验收三个页面调用。

**诚实说明**：我随后按几条路径反复尝试（直接切 tab、展开行内编辑器后切、切到「按摊位」后切）
**都没能再复现**。它跟当时的某个瞬时状态有关（刚通过审批、行内编辑器展开、按摊位分组同时成立）。
所以这一条我标为「已观测到一次、附完整堆栈、有明确嫌疑代码、但尚未稳定复现」。

修法本身是无风险的，无论是不是这次崩溃的原因都该做：把新建引用移出 selector（模块级常量 `EMPTY`，
或分开选 `session` 再在组件里取 `stores`）。同时建议在 CI 里加一条 lint 规则，禁止 zustand selector
里出现 `?? []` / `?? {}` / 对象字面量。

---

## 8. 建议的动手顺序

**先做（都很小，收益直接可见）** — ✅ 1–7 已完成，8 判断有误已撤销

1. ✅ 下单页 `QtyControl` 传 `unitLabel(sku.unit)`；`ReviewList`、`ConfirmPage` 同改。（§1.1）
2. ✅ `QtyControl.tsx:210` 数量 0 时渲染占位符，把那 64px 的隐形按钮变成可见控件。（§2.1）
3. ✅ `ApprovalPage.tsx:137` 的 `skuById` 补上 `step`，并把类型里的 `step?` 改成必填。（§3.5）
4. ✅ `ConfirmPage.tsx:177` 的 `allDecided` 改成真的「全部已判定」；`:297` 不再默认 ok。（§5.1）
5. ✅ `StoreSwitcher.tsx:120` 把 `?? []` 移出 selector。（§7）
6. ✅ 审核卡片恢复 `N 项 · ~金额`。（§3.1）
7. ✅ 采购页分组 chip 改用正确的 ARIA 角色（不是补选中态，见更正后的 §4.3）。
8. ❌ ~~金额 formatter 统一按币种取整~~ —— 判断有误，已撤销，见更正后的 §1.9。

**再做（本地化补齐）** — ✅ 全部完成

9. ✅ 硬编码英文进 catalog（实际 124 处，本轮清 45 处，其余被 ratchet 卡住）。(§1.2)
10. ✅ `QtyControl` 快捷弹窗全套字符串 + 无障碍标签进 catalog。(§1.2)
11. ✅ 日期/月份走 `i18n.locale`。(§1.3)
12. ✅ 新增 CI 检查 `scripts/check-untranslated.ts`。

**走查后才发现、已顺手修掉的**

- ✅ 审批人编辑自己提交的单会生成幽灵草稿（§3.6，原第 20 条）——
  含一个会真正失败的回归测试。
- ✅ navStore 持久化脏状态渲染空白页且刷新仍在（§10.9，走查时新发现）。

**结构性（第二轮：「定位与去向」已完成，详见 §11）**

13. ✅ 门店 chip 进粘性条；`×千` 移出常驻区、换成价格字段旁的单位切换。（§1.5、§1.8、§4.1）
14. ✅ 采购页「剩 N 项待处理」常驻下一步条，归零后变「开始配送」。（§4.2）
    14b. ✅ 采购页运行身份提权 + 过期标记（走查时新提的，§4.10）。
    14c. ✅ 崩溃屏文案本地化（原先归错优先级，见 §11.4）。

**仍待你拍板，未动**

15. 下单页提交后换成只读小票；加「今日已提交 N 批 ›」。（§2.3、§2.4）
16. 采购历史移出活动运行页，只留一行摘要。（§4.9）
17. 页面内补返回控件，别只靠 Telegram chrome；点当前 tab 回该 tab 的根。（§1.4）
18. 支付方式统一成一种控件；清掉「Phase 6 emoji sweep」的欠账。（§1.7）
19. 审批人补「新增漏报 SKU」的能力，并把改动展示为 `原值 → 新值`。（§3.7、§3.8）
20. 「总价 ÷ 数量」的舍入策略（从 §1.9 挪来 —— 要不要存原始总价？余数分给谁？）。

---

## 9. 本次走查留下的痕迹

需要清理或知悉：

- 本 worktree 新增了 `.claude/launch.json`（给预览工具用的 web 启动配置，`bun --bun x vite`）。
- 从主仓库复制了 `.env` 和 `apps/web/.env`（都在 .gitignore 里）。
- 本地 DB（`compass_pgdata` volume）里的 2026-07-30 记录：
  - §3.6 那张幽灵草稿（`9b636f4f-…`）**已删掉** —— 删掉之后下单页立刻正确显示出
    「已通过 · 等待采购」横幅，正是之前被它遮住的状态。
  - 为验证审核卡片的 `~金额` 渲染，给「土豆」插了一条 5,000 UZS 的近期价格观测
    （`inventory.price_history`，`observed_at = now() - 2 days`）。不需要就删：
    `delete from inventory.price_history where unit_price = 5000 and qty = 1;`
  - 单测 fixture 会留下 `slug like 'selfedit-%'` 的组织行（和既有测试一样不清理，
    便于事后取证）。
- API（:3000）和 Vite（:5173）还在跑，docker 里的 PG/Redis/MinIO/Adminer 也还在。

---

## 10. 已实施（2026-07-30）

全程 `bun --bun x tsc --noEmit` 逐包通过；`packages/domain` 83 测试、`apps/web` 194 测试、
`apps/api` 86 测试（含 PG-gated）全绿；每一项都在 375×812 浏览器里回归确认过。

### 10.1 单位标签统一（§1.1）

初版说 3 处，实际扫出 **10 处**，跨 7 个文件：

| 文件                     | 位置                                                    |
| ------------------------ | ------------------------------------------------------- |
| `OrderPage.tsx`          | SkuRow 的 QtyControl、`order.totalQty` 插值、ReviewList |
| `ConfirmPage.tsx`        | 收货行                                                  |
| `RunHistory.tsx`         | 历史明细 ×2、门店分账 chip                              |
| `RunSheets.tsx`          | AddItemSheet 的 SKU 选择行                              |
| `OperationsSections.tsx` | 价格报表的 `/ 单位` 后缀                                |
| `StoresSections.tsx`     | 库存 on-hand                                            |

只有 `<select value={row.unit}>` 保持原始值（表单值必须是 canonical），其余全走 `useUnitLabel()`。

### 10.2 数量 0 的隐形按钮（§2.1）

`QtyControl.tsx` 的 `{showZero ? '' : …}` 去掉，0 时渲染淡色 `0` + 单位。
量了一下宽度：内容最宽 41px，容器 64px（内边距后 56px 可用），无裁切。
副作用是每行现在都自报单位，把 M3.55 删掉的那行 caption 的信息补回来了，且没多占一行。

### 10.3 审核页步长恒为 1（§3.5）

`skuById` 补 `step`，并把 `SessionItems` 的 `step?: string` 改成**必填** ——
可选类型正是这个 bug 藏身的地方。两处 `Number(sku.step ?? '1')` 的兜底一起删掉。
验证：洋葱（step 0.5）从 1 点 `+` 现在得 1.5，修复前得 2。

### 10.4 验收环节的空转（§5.1）

- `allDecided` 改成 `myItems.length > 0 && undecided.length === 0`。
- `effectiveStatus` 不再 `?? 'ok'` —— 未判定就是没有选中项。
- 新增可见的门槛提示：卡片头一行 `已核对 3/12`（未完成时橙色），主按钮文案在
  未完成时变成 `还有 N 项待核对`。
- 快速通道做成**显式**的「全部无误」按钮：串行发 N 个 `confirmStoreItem`
  （同一 run 事件流，并发会撞 `(streamId, seq)` 唯一约束），过程中抑制逐条 toast、
  结束给一条汇总。

顺带发现：那段「Banner removed —— 已有 Decided X/Y 计数 + card meta 提示 + 禁用的主按钮」
的注释所引用的三样东西**后来全被删掉或失效了**。典型的 declutter 连锁反应。

### 10.5 selector 引用不稳定（§7）

全仓扫描 zustand selector，**只有 `StoreSwitcher.tsx:120` 一处**返回新引用
（其余 `?? null` / `?? false` 返回基元，`?? 'UZS'` 在 selector 外）。
改成模块级 `NO_STORES` 常量，附上崩溃堆栈的说明。
崩溃本身仍未能稳定复现，但这个修法与它是否为根因无关，都该做。

### 10.6 审核卡片的分诊信息（§3.1）

`itemCount` 服务端本来就在返回，只是前端不显示；预估金额是新加的。
为此把 `catalog.skuPriceStats` 里那段 30 行的 last/avg7d/avg30d 累加器抽到
`apps/api/src/services/priceStats.ts`，`order.pendingList` 复用同一套规则 ——
两份同样的算术早晚会算出不同的数。整页只多一次 price_history 扫描，不是每卡一次。

卡片右侧现在是 `N 项` + `~金额`，**没有**把旧的 `totalQty` 放回去（它是把公斤和个数
加在一起的无意义和，见 §4.5）。

### 10.7 Chip 语义（更正后的 §4.3）

见 §4.3 表格。`ChipBar` 的 `variant` 通过 context 下发给 `Chip`，
调用方无法让容器和子项的语义不一致。

### 10.8 本地化（§1.2、§1.3）

**新增 `UiLabelsProvider`（`packages/ui/src/labels.tsx`）+ `UiLabelsBridge`（app 侧）。**

`@compass/ui` 刻意没有 i18n 依赖（要能跑在任意 host），代价是它**自己渲染的每个字符串**
都被冻结成英文 —— 而这些组件在每一屏上：QtyControl 的无障碍标签和整个快捷选择弹窗、
DataState 的空/错状态、PhotoCapture 的提示。其中快捷弹窗是下单页录入数量的快路径，
也就是全 App 使用最频繁的交互，之前完全是英文。

给 8 个调用点逐个传 `labels` prop 也能修，但下一个调用点照样会忘 —— 这就是它变成现状的原因。
所以改成 host 在根节点填一次 context，primitive 各取所需；默认值保留英文，
`@compass/ui` 仍可独立使用。

**踩到一个值得记下的坑**：第一版在 `useMemo` 里**提前求值**每个字符串。
catalog 是懒加载的，而 `useI18n()` 返回的对象在加载前后**引用不变**，
所以 memo 永远不重算 —— 那些字符串在 zh catalog 落地前的那一帧拿到英文兜底，然后冻死。
函数型字段（`quantityPick`、`unitIs`）反而正常，纯粹因为它们在**调用时**才查表。
屏幕上的表现极具迷惑性：`单位: 公斤` 正下方一个死活不肯翻译的 `CUSTOM`。

修法：所有字段改成 **getter**，把函数字段偶然获得的延迟绑定给到每个字段。
配套地 `UiLabelsProvider` 的合并必须用 `Object.getOwnPropertyDescriptors`
而不是对象展开 —— 展开会**执行** getter 并存下结果，等于把刚做的延迟又冻回去。

其余已清：`CatalogSections`（全清）、`StoresSections`（按钮/标题/确认框/四个子标签）、
`PeopleSections` 成员元信息行。后者尤其能说明问题：
`admin.label.you` / `.noTelegram` / `.neverSeen` **四个 catalog 里早就有译文**，
只是代码从未用过，一直渲染英文字面量。

日期：新增 `useDateFormat()`（按 locale memo 化 `Intl.DateTimeFormat` 实例），
替换 6 处 `toLocaleDateString(undefined/[])`。`DebugPage` 不动（operator-only，有意英文）。

### 10.9 走查后新发现并修掉的两个缺陷

**a) 审批人编辑自己提交的单会生成幽灵草稿（§3.6）**

`order.ts` 的 `isManagerEdit` 判定去掉多余的 `!== me` 子句（`targetMemberId`
只由审核界面发送，它的**存在**就是意图信号），lazy-create 守卫同步；
manager 分支优先查「本人已认领的 submitted」再回落，避免 approver == submitter 时
`orderBy updatedAt` 抓到自己无关的草稿。`ApprovalPage` 补传 session 自己的 `orderDate`
（原先默认今天，改前天的单会查错日期）。

新增 `apps/api/src/__tests__/approver-self-edit.test.ts`。
**把修复临时回退验证过：两个测试都会失败**，不是空跑的测试。

**b) navStore 脏状态渲染空白页（新发现）**

`persist` 无校验地 rehydrate localStorage。类型上 `adminSection` 不可为 null、
action 也产不出不一致组合，但**存储里的内容在类型系统之外**：旧版本残留、
写入中断、手工改键、字段改名都会原样进来。

实测 `adminSection: null` + `catalogSub: 'skus'` → 管理页只剩一个空 `<h1>`，
而且因为管理页下钻没有页面内返回控件（靠 Telegram chrome，见 §1.4）、脏值又是持久化的，
**刷新还在同一个空白页**，除了清 localStorage 没有出路。

在 `merge` 阶段加清洗：未识别值回落默认，子选择随父 section 一起清空。
与本文件自己的 `resolveVisibleTab` 和文件头承诺的「stale-tab guard」是同一套思路。
已投毒验证：脏状态现在正确落回管理首页。

### 10.10 新增的 CI 检查

`scripts/check-untranslated.ts` —— 已接入 `.github/workflows/ci.yml`，
紧跟现有的 i18n parity 步骤。

四类检测：JSX 裸文本节点、`title`/`ariaLabel`/`placeholder` 等 prop 字面量、
三元分支里的 sheet 标题、`nativeConfirm` 的英文正文。

第一版对表达式位置的字面量误报很多（Tailwind class、`'wss'`/`'numeric'`/`'muted'`
这类判别式标签），因为真实文案 5 倍地被噪音埋掉。所以表达式位置多加一道门：
必须首字母大写、且不含 CSS/选择器标点。JSX 文本节点不需要这道门（那里只可能是文案）。
另有 `// i18n-exempt: <理由>` 行内豁免，**理由必填** ——
下一个读代码的人得能分清「有意语言中立」和「没人来得及处理」。

**是 ratchet，不是硬门禁**：`BASELINE = 79`，只允许降不允许升。
剩下的 79 处全在管理页深处（角色管理、邀请、成员转移、清理工具、Operations 空态）。
带着这个欠账直接设成硬失败，只会让所有人习惯性忽略红色 CI。
清到 0 就把 ratchet 删掉，改成任何一处都失败。

---

## 11. 定位与去向（2026-07-30，第二轮）

一组改动，一个共同毛病：**App 知道，但不告诉用户**。全部在 375×812 下逐屏验证过。

### 11.1 门店 chip（§1.5，原第 13 条一半）

新增 `StoreChip`（`components/StoreSwitcher.tsx`），接入下单 / 审核 / 验收三个粘性条。

设计上和 M3.5 拆掉的那个 pill 有一点关键区别：**它首先是标签，其次才是入口**。

- **所有人可见** —— 「我在给哪家店操作」是上下文，不是权限。
- **只有 `org.admin` 可点** —— 其他人拿到的是一段没有交互暗示的静态文字，
  不会去点一个点不动的东西。
- 可点时，picker 装在 **chip 自己的 sheet** 里，而不是把人丢去设置页里翻。
  顺带给 `StorePickerSection` 加了 `showLabel`（默认 true，保持设置页原样）——
  在 chip 的 sheet 里标题已经写着「当前门店」，不需要再印一遍。

M3.5 当初的理由（只有老板需要切换，不必每页都放）对**切换**是成立的；
它连带拿掉的是全 App 唯一说出「当前门店」的地方。

**实测效果**：切到 Eden Seoul，页面从「洋葱 0.5 公斤 + 已通过横幅」变成全零、横幅消失。
改之前，同样的操作会产出一个**看起来一模一样的页面**，没有任何提示。

顺手更新了 `storeSwitcher.pickPrompt.body` —— 它原本写着「请打开设置（齿轮图标）选择」，
chip 上线后这句话就成了错的指路。

**留个尾巴**：审核页在锁定单一门店时，chip 和每张卡片的标题会重复显示同一个门店名。
可以考虑 `specific` 模式下把卡片标题换成提交人（顺带解掉 §3.2 的头像/标题错配），
但那会让两种模式下的卡片结构不一致，是个设计决策，没动。

### 11.2 `×千` → 价格字段旁的单位切换（§1.8，原第 13 条另一半）

**没有**按初版建议做成分段控件。原因是量了一下宽度：Telegram 里
`--app-chrome-pad-left: 56px` + `right: 96px` 吃掉 152px，375 宽的粘性条**只剩 ~223px**
（我的浏览器截图 pad 是 16px，看不出这个问题 —— 现有的 bar 在真机上本来就已经挤了）。
再塞一个更宽的带文字控件只会更糟。

所以把控件**移到价格输入框右边的单位后缀上**（`K·UZS` / `UZS`，蓝色虚线下划线）：

- 在用户**正要输价格**的时候就在眼前，而不是屏幕另一头的一个缩写；
- 后缀陈述的是**当前单位**，不是「点我会做什么」——「20 K·UZS」自己就说明白了；
- 粘性条腾出宽度给运行身份（下一条）。

而 `+新增` / 弹窗里的价格字段本来就有完整文字标签（`单价 (千 UZS)`），从来不是问题所在 ——
这也反过来印证了方向：**陈述单位，而不是命名操作**。

改完暴露出两个连带问题，都修了：

1. **明文 UZS 模式下价格输入框只有 46px**，`30000` 被截成 `300`。
   价格那一栏原本是 `minmax(0,1fr)` —— 又贪婪又能被压缩到 0。
   在千位模式下价格只有 2-3 位，谁也没注意；这大概正是当初把 ×1000 设成默认开启的原因。
   现在给了 4.25rem 下限。
2. **`= 总价` 回显被挤没了**。七个栏位在 375px 下装不下 5-6 位的价格。
   它是**只读回显**，而且正是防 ×1000 手误的那道保险，所以**移到下面单独一行**，
   右对齐，永远不会被截断。（旁边支付按钮的注释专门写过
   「不要加宽，除非给第 3 栏一个下限」—— 我给了下限，也顺手更新了那条现在已经过时的注释。）

两个模式都验证过：千位 `30 K·UZS` 和明文 `30000 UZS` 都显示 `= 30,000 UZS`。

清掉了两个随之失效的 key（`run.label.thousandsToggle` / `…Aria`）。

**取舍要说明**：切换只在展开的采购行里可达。`planned` 状态下没有展开行，就切不了。
可以接受 —— 这个设置只在录价格时才有意义，而它是持久化的（设一次就记住）；
弹窗自己有明确的单位标签，不存在歧义。

### 11.3 采购页常驻下一步条（§4.2，原第 14 条）

新增 `pendingItemCount()`（`runs/lib/runProgress.ts`，附 3 个单测），
和 `allItemsHandled` 用**同一个谓词**取反计数，两者不可能对「什么算已处理」产生分歧。

`purchasing` 状态下主按钮不再隐藏：

- 未处理完 → `还有 N 项待处理`，**可点**，跳到第一个待处理行
- 归零 → `开始配送`

跳转靠 `<li data-run-item-pending>` 标记 + `scrollIntoView`。用属性而不是 ref 链，
因为这些行由四个不同的视图组件渲染（平铺 / 按门店 / 按摊位 / 按类型），
为一个 scroll-into-view 把 ref 串过四层不值得。

2026-05-04 隐藏它的理由是「用户从行标记就知道还剩什么」。85/87 未处理时这个假设站不住：
行标记是个 7px 的圆环、散布在 ~7,000px 里，「剩 85」计数器跟着段头一起滚走了，
而唯一始终在视野里的底部条是空的。`runProgress.ts` 自己的文件头就点了这个雷 ——
「答错了不是禁用按钮，是删掉按钮，把采购员困在原地」。

**实测**：点「还有 85 项待处理」→ `main.scrollTop` 0 → 556，停在第一个待处理行（东干辣椒）。

### 11.4 运行身份提权 + 过期标记（§4.10）

`#1 · 2026-07-27 · 采购` 原本是全屏最小、最灰、最右的元素 —— 而它是下面所有内容的定语
（每一份提交、每一个商品、每一个价格都属于**这一张**采购单）。
×千 腾出宽度后给了它正常字重和深色，并挪到左侧。

加了过期标记：`3 天前`（warn 底色）。我走查时那张活动采购单是 3 天前的、状态还停在
`purchasing`，全屏没有一个字提到这件事 —— 采购员打不开 App 就分不清「正在进行的行程」
和「某人忘了关的单子」。

为了让三样东西在 ~223px 里共存，当年份是今年时日期省掉年份（`07-27`），
过期标记也压到最短（`3 天前`）。最终：`+ 新增 | #1 · 07-27 · 采购 | 3 天前`。

### 11.5 崩溃屏文案本地化

这条**我一开始归错了优先级** —— 把 `ErrorBoundary` 的两句英文和「管理页深处」归成一类。
其实它是 App 崩溃时用户看到的**唯一**界面，优先级远高于角色管理里的按钮。

`ErrorBoundary` 是类组件，而且是整棵树最外层（包着 `ThemeProvider` 和 `UiLabelsBridge`），
所以既用不了 hook 也读不到任何 context —— 它是这些东西的祖先。
新增 `crashLocale()`：优先读持久化 session 里的 `user.locale`（用户真正选的语言），
回落到 Telegram `language_code` / `navigator.language`。
`createI18n` 是纯函数，`lookup` 会穿透回 `en`（静态打包），所以启动期崩溃降级成英文而不是显示 key。

**两种情况都实测了**（临时在 OrderPage 里塞了个 throw，验证完已移除）：

- 启动期崩溃 → 英文（zh catalog 还没加载完，这是诚实的降级）
- 会话中崩溃 → 全中文「应用启动时出错了。/ 重试 / 清除登录状态并重新加载」

后者是现实中会发生的那种（我 7 月 30 日撞到的那次就是切 tab 时崩的）。

### 11.6 一次操作失误，已修复

中途我用 `git checkout --` 回退两个文件里一次过宽的批量替换，
连带把 §10.7 的 chip variant 改动（RunPanels 里的 `variant="toggles"`）一起清掉了。
已重新加回并确认。写在这里是因为它提醒我：这轮改动全部未提交，
`git checkout --` 在这种状态下没有安全网。

### 11.7 验证

| 项目                     | 结果                                                           |
| ------------------------ | -------------------------------------------------------------- |
| `tsc --noEmit` × 11 个包 | 全部干净                                                       |
| `packages/domain`        | 83 pass / 0 fail                                               |
| `apps/web`               | 197 pass / 0 fail（比上一轮 +3，新增 `pendingItemCount` 测试） |
| `apps/api`               | 86 pass / 0 fail（含 PG-gated）                                |
| i18n 四语对齐            | 969 × 4                                                        |
| 裸英文 ratchet           | pass（79，未增长）                                             |
| 浏览器控制台             | 五个 tab 循环，无报错                                          |

---

## 12. 一个根因，六处误判（2026-07-30,第三轮)

本来打算做下单页 IA。动手前先探了一下现有 API 面,结果撞到一个**比原计划所有条目都严重**的东西 ——
两个已上线的管理功能**完全不可用**。所以改了顺序,先修这个。

### 12.1 根因

`index.html` 用**静态 script 标签**引了 `telegram-web-app.js`。
所以 `window.Telegram` / `getTg()` 在**任何浏览器里都为真** ——
它回答的是「SDK 加载了吗」,而**不是**「我现在真的跑在 Telegram 里吗」。

有六处拿它当后者用,于是全部一致地走进了「Telegram 在,它会提供这个控件」的分支:

| 位置                       | 原判断               | 后果                           |
| -------------------------- | -------------------- | ------------------------------ |
| `CatalogSections.tsx:1505` | `!getTg() && draft`  | **菜品无法新建/编辑**          |
| `StoresSections.tsx:547`   | `!getTg() && action` | **盘点/损耗无法提交**          |
| `QtyControl.tsx:493`       | `!!getMainButton()`  | 数量弹窗自定义值无法确认(§1.6) |
| `AdminPage.tsx:420`        | `!!getTg()`          | 管理页下钻没有返回(§1.4)       |
| `Shell.tsx:180`            | `!!window.Telegram`  | 从不渲染自己的 header          |
| `OrderPage.tsx:849`        | `!getTg() && …`      | 死代码(见 12.4)                |

前三处指望的是 Telegram 原生 MainButton —— 而 **M3.49 已经把它永久 `hide()` 了**,
改用 DOM 内的 `PageMainButton`。M3.49 当时在 ConfirmPage 和 RunSheets 做了正确的迁移,
注释写得明明白白:

> M3.49: dropped `!getTg()` — the in-page PageMainButton sits behind any open sheet,
> so we ALWAYS render the sheet's own footer button now. Was `!inTelegram`
> (Telegram MainButton was the canonical CTA), **no longer correct**.

**这三处被漏掉了。** 它们既没有 footer 按钮,也没有 `usePageMainButton` 注册
(整个 `pages/admin/` 目录里一次都没调用过),原生按钮又被隐藏 —— 于是三个 sheet 在**所有环境下**
都没有提交按钮。

### 12.2 菜品创建/编辑:浏览器实测为完全不可用

打开「+ 新菜品」,枚举弹窗里所有按钮:

```
before: ["+ 添加食材"]          ← 没有保存
after:  ["+ 添加食材", "保存"]
```

修好后走通了完整链路:填四语名称 + 编号 + 售价 → 保存 → 弹窗关闭 → 列表出现 →
**DB 里确认落库**(`inventory.dishes` code=`UIUX-1`,已清理)。

盘点/损耗是同一段代码形状(`!getTg() && action`),但那家店没有库存数据、按钮不渲染,
所以我**只做了代码层确认,没有浏览器实测** —— 这里说清楚,别当成两个都跑过。

### 12.3 新增 `isInTelegram()`

`useTelegram.ts` 里加了一个诚实的判断:`initData` 非空。
Telegram 只在页面作为 Mini App 打开时才注入这个签名串 —— `AuthGate` 从 M1 起就一直用它做 dev-bypass 判断,
所以这个信号在仓库里本来就有先例,只是没被推广。

分工写在 docblock 里:

- `getTg()` —— 只是要**调用** SDK(haptics / showConfirm / MainButton.hide),
  在 Telegram 外面 no-op 无害。
- `isInTelegram()` —— 答案会改变**渲染什么**。

用它修了两处:管理页返回按钮、Shell 的 header。顺手把返回按钮里硬编码的 `Admin` 换成 `i18n.t('nav.admin')`。

**实测**:`Compass / Default Organization` 头部出现了(之前是每屏顶部 ~60px 的空白),
`‹ 管理` 返回按钮出现,两级返回都验证过(菜品 → 目录 → 管理首页,`adminSection` 正确回到 `home`)。

这也让 §1.4 那个死胡同从两头都堵住了:上一轮修的是「脏状态进来怎么办」,这一轮修的是「正常状态怎么出去」。

### 12.4 QtyControl 不再抢原生按钮

`QtyControl` 的快捷弹窗彻底不碰原生 MainButton 了,永远渲染自己的确认按钮,
连带删掉了 115 行已经没有意义的 `useTelegramMainButton` / `getMainButton` 辅助代码。

一次修掉 §1.6 的两个后果:真机 Telegram 里不再把已退役的原生按钮召回来(那会把底部导航顶起,
正是 M3.49 要消除的抖动),浏览器里自定义数量也有确认按钮了。

**实测**:输 `7.5` → 按钮变成可点的「设为 7.5 公斤」→ 点击 → 弹窗关闭 → 行显示 `7.5 公斤`。

### 12.5 OrderPage 那处是删,不是改

`OrderPage:849` 的 `!getTg()` 兜底提交按钮和其他五处方向相反:它**不该**被"修正"。
M3.49 之后 `PageMainButton` 在所有环境都渲染,已经承载了同一个动作 ——
把谓词改对只会让按钮出现两次。所以直接删掉,注释里写明为什么这一处的正确处理是删除。

### 12.6 验证

| 项目                   | 结果                                  |
| ---------------------- | ------------------------------------- |
| `tsc --noEmit` × 11 包 | 全部干净                              |
| `packages/domain`      | 83 pass / 0 fail                      |
| `apps/web`             | 197 pass / 0 fail                     |
| `apps/api`             | 86 pass / 0 fail(含 PG-gated)         |
| i18n 四语对齐          | 969 × 4                               |
| 裸英文 ratchet         | **79 → 77**,已按脚本提示下调 baseline |
| 五 tab 循环            | 无控制台报错                          |

### 12.7 这轮暴露的一个模式

三次里有两次(§1.6 的 `!!getMainButton()`、这一轮的六处)是同一件事:
**把「能力存在」当成「环境是这个」**。M3.49 是一次正确但没做完的迁移 ——
它改对了两个 sheet,留下三个,而留下的那三个恰好没有任何测试或类型检查能发现。

值得考虑加一条护栏:禁止在 `apps/web/src` 里直接用 `getTg()` 做渲染分支
(类似现有的 typography guard 那种 grep 检查),强制走 `isInTelegram()`。
我没加 —— `getTg()` 有大量合法用途,要区分「用于调用」和「用于分支」得看上下文,
grep 判不出来,容易变成噪音。留给你判断值不值得。

---

## 13. 下一步该问你的

按我的判断排:

1. **舍入策略(§1.9)** —— 唯一一条我不该替你决定的。`70,000 ÷ 3` 存成 `23,333.30`,
   乘回去 `69,999.90`,每笔丢 10 som 并顺着门店分账、月合计扩散。
   要答的是:**(a)** 要不要额外存「实付总价」这个字段(改 schema + domain 事件)?
   **(b)** 除不尽的余数给谁 —— 第一家店、最大那家、还是按最大余数法分摊?
   给了规则我就能做。

2. **下单页 IA(§2.3/2.4)** —— 本来这轮要做的。提交后看不到自己报了什么;
   后端支持一天多批而前端只显示一批、静默覆盖。要答的是:
   「一天多批」到底要不要给用户看见?要,就加「今日已提交 N 批」;不要,就在提交后
   直接锁成小票、不允许再开新批。

3. 剩下的(历史移出、支付方式统一、审批人补录 SKU、77 处深层管理页 i18n)
   都是明确的活,不需要你先拍板,我可以按顺序推。

---

## 14. 两个开放决策,我定了(2026-07-30,第四轮)

### 14.1 §1.9 舍入策略 —— 结论:不是缺陷,不改

先读了 `settlement.ts`,发现一件推翻我自己结论的事:**全部结算都是 `qty × unitPrice`,
没有任何地方拿总价去除**。分账切的是 **qty**,不是价格 ——
`splitSubtotal = Number(split.qty) * Number(unitPrice)`,所以只要 Σqty 等于 purchasedQty,
分账之和就精确等于总额,分摊本身零损耗。

那 `23333.30` 是哪来的?**采购员自己心算除出来再填进去的。** 应用只是忠实记录。

于是我去量了全库:

```sql
select count(*) as lines,
       count(*) filter (where (purchased_qty*unit_price) <> round(purchased_qty*unit_price)) as non_integer,
       sum(abs((purchased_qty*unit_price) - round(purchased_qty*unit_price))) as total_residual
from read_model.run_items_v where status='purchased' and unit_price is not null;

 lines | non_integer | total_residual
-------+-------------+----------------
  1735 |           1 |        0.10000
```

**1,735 条采购行里只有 1 条乘积非整,全库残差合计 0.10 UZS**(约 0.000008 美元)。

所以我在 §1.9 写的「每笔丢 10 som,会顺着门店分账和月合计一路扩散」——
**量级错了一个数量级以上,而且「扩散」也不成立**(误差随 qty 线性,始终亚 som 级)。
这是我这几轮里第三个需要更正的结论,记在这里。

**决策**:不动 schema,不加舍入策略,也**不**加「输总价反推单价」的入口。
后者听起来是对症的,但反推同样要除(70000/3 → 23333.33,乘回去 69,999.99),
只是把舍入从人手里挪到机器手里;而代价是在**提交金钱的那一行**多一个输入框,
引入「哪个字段是权威」的歧义。为 0.1 som 做这个交易不值得。

留下这段测量,是为了让下一个人不用重新怀疑一遍。

### 14.2 多批次 —— 决策:露出来

后端 M3.32 特意支持一天多批,理由很实际:下午想起早上漏报了。藏掉等于砍能力。
所以选择「露出来」,并把它和 §2.3 那个功能缺口一起解决。

**新增 `order.todayBatches`**(`order.ts`)。`todaySession` 只返回一条 —— 这对「我在编辑什么」
是对的答案,但它也是下单页知道的全部,于是 M3.32 的多批完全不可见:
提交一批、再开一批,第一批就无声消失。

批次编号按 `submittedAt ASC NULLS LAST` 排。这个字段一旦写入就不变,所以**批次号在用户继续编辑时不会跳**;
未提交的草稿(null)排最后、拿最大号,正好对应「我现在在弄的这一批」。
这个 read model 没有 `createdAt`,而 `updatedAt` 会让列表每敲一下键就重排。

**提交后改为只读小票**(`OrderPage`)。原先提交完留在同一个 244 行的禁用目录上 ——
约 12,000px 的灰色列表 —— 而复核弹窗唯一的入口是主按钮,它在 `canSubmit` 变 false 后把自己隐藏了。
所以按下提交之后,**没有任何路径能看到自己刚报了什么**。

目录是用来**做选择**的工具;选择做完之后它就是噪音。现在换成小票:
报了什么、值多少、走到哪一步、以及此刻你可能想做的两件事(撤回 / 再报一批)。

`ReviewList` 是故意复用的 —— 提交前的预览和提交后的小票应该是同一个东西,
不能让人觉得东西在路上变了样。

**「再报一批」** 把 `todaySession` 缓存置 null,于是 `handleQtyChange` 走它的 `if (!old)` 分支
建一个干净的乐观草稿,而不是往已提交的那条上追加;真正的草稿由服务端在第一次 AdjustItem 时惰性创建。
切换门店会清掉这个模式 —— 另一家店有它自己的 session 和它自己的「我在编辑吗」。

**顺手修掉一处硬编码英文**:原来的「正在审核中」横幅写的是 `who: 'manager'` ——
一个英文字面量,而且正是 M3.22 在审批队列里修过的「没人知道是谁压着这单」问题。
给 `todaySession` 补上 `claimedByDisplayName`(`pendingList` / `sessionDetail` 早就有了)。

**完整链路实测**:

| 步骤           | 结果                                                                                                       |
| -------------- | ---------------------------------------------------------------------------------------------------------- |
| 提交后         | 小票:`Eden Magic City ▾ · 7月30日星期四` / `已通过 · 等待采购` / `你报的内容 · 洋葱 0.5 公斤` / `再报一批` |
| 点「再报一批」 | 目录回来、可编辑、全部归零                                                                                 |
| 加项 → 提交    | `今日 2 批`:`第 1 批 已通过 · 1 项` + `第 2 批 已提交·等待审核 · 正在查看 · 1 项 · ~2,500 UZS`(当前批高亮) |
| 点「撤回」     | 回到可编辑草稿,`今日 2 批` 仍留在粘性条                                                                    |

第 1 批不显示预估金额(洋葱近 30 天没有价格观测)—— 这是诚实的行为,不是缺数据的 bug。

### 14.3 一个假警报,值得记

改完之后浏览器一片空白,控制台 `ReferenceError: BatchStrip is not defined`。
但 `tsc --noEmit` 是干净的,而且 `BatchStrip` 确实在模块顶层(函数声明会提升)。
错误栈里 `OrderPage.tsx?t=1785402285536` 的时间戳和其他文件不一致 ——
**Vite 部分 HMR 更新**:引用了新组件的 JSX 进来了,组件声明本身没进。整页刷新即恢复。

写在这里是因为我第一反应是「我写错了」,而正确的第一步是**看时间戳对不对得上**。

### 14.4 验证

| 项目                   | 结果                                     |
| ---------------------- | ---------------------------------------- |
| `tsc --noEmit` × 11 包 | 全部干净                                 |
| `packages/domain`      | 83 pass / 0 fail                         |
| `apps/web`             | 197 pass / 0 fail                        |
| `apps/api`             | 86 pass / 0 fail(含 PG-gated)            |
| i18n 四语对齐          | 975 × 4                                  |
| 裸英文 ratchet         | 77,持平                                  |
| 测试数据               | 已清理(探针菜品、两批测试订单、幽灵草稿) |

### 14.5 现在剩什么

**都是明确的活,不需要再拍板:**

1. 采购历史移出活动运行页,只留一行摘要跳到已有的「全部历史」(§4.9)——
   能从采购页去掉约 8,000px。
2. 支付方式统一成一种控件,清掉「Phase 6 emoji sweep」欠账(§1.7)。
3. 审批人「新增漏报 SKU」+ 改动展示为 `原值 → 新值`(§3.7、§3.8)——
   要加 domain command,这几条里最大的一个。
4. 77 处深层管理页 i18n(PeopleSections 42 / OperationsSections 28 / 其余 7)。
5. 审核卡片在锁定单店时和门店 chip 重复显示门店名的小尾巴(§11.1 末)。

**我建议的顺序**:1 → 2 → 5 → 4 → 3。前三个都是收敛改动、当场能看见效果;
i18n 是纯机械活适合成批做;补录 SKU 动 domain,留到最后单独一轮。

---

## 15. §14.5 的前三件(2026-07-30,第五轮)

### 15.1 采购历史折叠成一行 —— 顺带更正一个数字

原先内联最多 12 单、按月分组,位置在采购页 ~8,161px 处 —— 要看上月花了多少,得先滚过 87 行
正在处理的采购。而 header 里一直有个「全部历史」链接,通往一个已经有分页、搜索、筛选的独立视图,
所以内联那份是在复制一个更好的界面。

现在是一行摘要:

```
历史记录                          全部历史
最近一单 2026-06-25               87,089,399.9 UZS
2026年6月 · 已 26 单
```

**两个 tap 目标各自名副其实**:行本身指名了一张具体的采购单,所以点它打开**那一单的详情**;
header 的「全部历史」打开列表。

**顺带一个改进**:原来的月合计是从 12 条的切片里算的(June 显示 65,866,899.9),
去掉 `HISTORY_INLINE_CAP` 之后是全量 26 单(87,089,399.9)—— 数字变了,因为之前那个是错的。
不过 `runs` 本身仍是列表查询返回的范围,所以月合计覆盖「我们拿到的单」而非「这个月所有单」。
这个局限是继承的,不是新引入的,但我在代码注释里写明了 ——
一个醒目的大数字比十二个小数字更容易被当成权威。

**更正**:我上一轮说这能「从采购页去掉约 8,000px」。实测 **9,377 → 8,244px,去掉 1,133px**。
8,161 是历史区块的**位置**,不是它的**大小** —— 我把两者搞混了。页面的主体仍然是那 87 行商品。

### 15.2 支付方式:一条规则替代三种表达

原先三种:弹窗里 `💵 现金`(带字,好)、采购行内联纯 `💵`(意思只在 `title` 里)、
汇总行 `💵 6,066,500`(纯 emoji 当图例)。

`RunViews` 里有段注释其实已经承认了问题:

> colour emoji ignore the ink token, so 💵 vs 🏦 alone is a weak signal at arm's length in sunlight

作者当时用容器底色去补偿。但**底色补偿的是可见度,不是可理解性** —— 你仍然得先知道 💵 代表现金。

**现在的规则:支付方式永远用文字写出来;💵/🏦 只能出现在文字旁边,不能替代文字。**

改了 8 处纯 emoji 承载语义的地方:采购编辑器的 toggle、只读的按店指示、行级 toggle、
采购历史图例、完成采购的确认摘要、运营报表 3 组图例 + 2 处行内前缀。

`现金`/`转账` 是两个 CJK 字,在 `text-label` 下约 22px,**比 emoji 更窄**,
所以塞进原来那个 36px 的槽位反而更宽松。容器底色留着,现在是冗余强化而不是唯一信号。

同时更新了两处已经过时的注释:`PaymentMethodChips` 的「Phase 6 emoji sweep」欠账标记,
和 `RunViews` 里「Tooltip on long-press spells out the active label」——
触屏上没有 long-press tooltip,那句话从来没成立过。

**实测**:点 `现金` → 变 `转账`,`aria-pressed` 同步,`aria-label` 为「支付方式: 转账」。

### 15.3 审核卡片:哪个名字当标题,取决于页面已经说过什么

门店 chip 上线后,锁定单一门店时 chip 和每张卡片的标题重复显示同一个门店名 ——
标题那行花在复述上下文,而卡片之间**真正不同**的东西(谁提交的)被压进了灰色 meta。

规则:**标题放页面还没告诉你的那个名字。**

| 门店上下文 | 标题   | meta                   |
| ---------- | ------ | ---------------------- |
| 锁定单店   | 提交人 | 日期(+ 多人时贡献者数) |
| 所有门店   | 门店名 | 由 X 提交 · 日期       |

顺带解掉了 §3.2 的头像错配:头像 `src` 是**提交人**的照片,但 `name`(同时提供 alt 文本和首字母兜底)
之前给的是**门店名** —— 读屏软件会把一张人脸念成「Eden Magic City」,而没有头像的提交人会顶着门店的首字母。
现在 `name` 也是提交人,两者终于指同一个人。

**实测**:锁定单店 → 标题 `Nurzat`、`alt="Nurzat"`、门店名只出现在 chip;
切到「所有门店」→ 标题回到 `Eden Magic City`、meta 变回 `由 Nurzat 提交 · 2026-07-27`。

### 15.4 两次 HMR 假警报

这轮又撞到一次空白页。和 §14.3 同一个原因:改了多个文件后 Vite 只应用了部分 HMR 更新。
第一次我花了几分钟怀疑自己的代码,第二次直接整页刷新就好了。

**教训写下来**:改动跨多个文件之后,先整页刷新再测,不要相信 HMR。
判断方法是看错误栈里各文件的 `?t=` 时间戳对不对得上。

### 15.5 验证

| 项目                   | 结果                          |
| ---------------------- | ----------------------------- |
| `tsc --noEmit` × 11 包 | 全部干净                      |
| `packages/domain`      | 83 pass / 0 fail              |
| `apps/web`             | 197 pass / 0 fail             |
| `apps/api`             | 86 pass / 0 fail(含 PG-gated) |
| i18n 四语对齐          | 977 × 4                       |
| 裸英文 ratchet         | 77,持平                       |
| 五 tab 循环            | 无控制台报错                  |
| 采购页高度             | 9,377 → 8,244 px              |

### 15.6 剩下两件

1. **77 处深层管理页 i18n** —— PeopleSections 42 / OperationsSections 28 / 其余 7。
   纯机械活,ratchet 卡着不会变多。清完就能把 ratchet 删掉改成硬门禁。
2. **审批人「新增漏报 SKU」+ 改动展示为 `原值 → 新值`**(§3.7、§3.8)——
   要加 domain command(事件类型 + reducer + decide + projector + tRPC + FE,
   按 README 的标准流程走),这几轮里最大的一个,值得单独一轮。

按之前定的顺序,下一个是 i18n。

---

## 14. i18n 收尾：从 77 到 0,以及路上挖出的两个真 bug（第四轮）

目标是把 §10.10 里那 77 处欠账清完。清完了,但过程里发现**记分牌本身在骗人**,
而且撞出两个比欠账本身更严重的问题。

### 14.1 记分牌漏报三分之一

77 是「语法锚定」的计数 —— 规则挂在 `prop=`、三元字面量、`confirm()` 参数、
以及「整行只有裸单词」的 JSX 文本上。它抓得到 `<Banner title="Read-only">`,
**抓不到紧挨着它的正文段落**:

```jsx
<Banner tone="info" title="Read-only">
  {' '}
  ← 被计入 77 You don't administer this store. ← 没被计入 Settings are visible but can't be… ←
  没被计入
</Banner>
```

几乎每个被标记的 Banner/EmptyState 标题下面都躺着一到三句没翻译的正文。
真实欠账大约是 115 处,不是 77。

我写了第五条「散文规则」想把它们也抓出来,**然后删掉了**。每一版都在用一类误报
换另一类:JSX 注释的续行、跨行 import 的成员列表
（`pendingItemCount as pendingItemCountOf,`）、`.ts` 文件里的普通字符串。
加了注释块追踪和 import 追踪之后还是不收敛 —— 最后一版改用「必须含英语虚词」
做判别（真句子有 the/is/you/this,标识符列表没有），误报确实降了,但又开始漏报。

**结论:基于行的扫描分不清「JSX 文本位置」和「注释正文」,不解析就做不到。**
一个会误报的守卫比没有守卫更糟,所以规则五整个撤掉,
把它带来的那条洞见写进脚本注释留给下一个人:

> **被标记的 Banner/EmptyState 标题旁边几乎一定有没翻译的正文。**
> 修 `prop:title` 命中时,顺手读一遍周围那段。
> 下面的计数是「锚定字符串」,不是「全部欠账」—— 别把「0 findings」读成「全都翻译好了」。

保留下来的注释块追踪和 import 追踪对规则 1–4 是净收益（精度提升,计数不变）。

### 14.2 撞出来的真 bug 一:ICU 复数**从来没生效过** 🔴

给新的计数类文案写了 ICU 复数,然后按四语验证了一遍 —— 发现 en 和 ru 渲染成**只有数字**:

```
people.toastRolesRevoked  n=5  ->  "5"        （想要 "5 roles revoked"）
```

追下去,`packages/i18n/src/index.ts` 里有个叫 `stripPluralWrappers` 的函数,
把每个 `{n, plural, ...}` 块**整块替换成裸计数**。它的注释说这是「安全网」,
因为更早一次正则实现渲染出了乱码,等 M2 换 `@formatjs/intl-messageformat` 再说,
在那之前「call sites must branch on count themselves」。

**但没人照做。** 仓库里已经有 24 个 en / 20 个 ru / 8 个 uz 的 key 是 ICU 复数写法,
每一个都在静默丢掉名词:

| key                          | n   | 实际渲染 | 应该是           |
| ---------------------------- | --- | -------- | ---------------- |
| `approval.contributorsCount` | 3   | `3`      | `3 contributors` |
| `run.label.sessionsCount`    | 4   | `4`      | `4 sessions`     |
| `run.label.itemsHint`        | 7   | `7 · x`  | `7 items · x`    |
| `admin.label.memberCount`    | 5   | `5`      | `5 members`      |

**zh 一个复数块都没有** —— 中文不按数量变形 —— 这正是它能藏这么久的原因:
主语言看起来完美无缺,而英文和俄文界面一直在掉名词。审核卡片在英文下读作
`由 Nurzat 提交 · 3 · 2026-07-27`。

**修法:实现它,而不是绕开它。** 那个「安全网」里的嵌套花括号遍历器写得是对的 ——
它能正确找到块边界,只是找到之后丢掉而不是选择。改成解析 case 分支并用
`Intl.PluralRules` 选一个,约 30 行,零调用点改动,顺手把一条 M2 TODO 结掉。
`Intl.PluralRules` 是运行时自带的,它已经知道每种语言的规则。

为什么不反过来改写那 24 个 key 成「Contributors: 3」这种不随数量变形的说法:
俄语有三种形式（одна роль / две роли / пять ролей）,再怎么改写
`{n} ролей` 在 n=1 时都是错的。选择必须发生在某个地方。

### 14.3 撞出来的真 bug 二:俄语 `one` 不只匹配 1

给新引擎写单测时,有一条**红了**:

```
Expected: "21 участник"
Received: "1 участник"
```

不是引擎错,是**词条写错**。俄语 CLDR 的 `one` 类别覆盖 1、21、31、101…,
而词条正文里写死了数字:

```
'{n, plural, one {1 участник} few {{n} участника} other {{n} участников}}'
                  ↑ 写死的 1
```

n=21 时选中 `one`,渲染出「1 участник」。8 个 ru 词条 + 1 个 en 词条有这个问题。
全部改成 `#`（引擎会替换成计数）。

这条我要强调:**是我自己写的测试抓到的,不是我看出来的。**
如果只按「四语各看一眼 n=3」验证,这个 bug 会活得好好的。

加了一条 CI grep 守住它:分类 case 的正文必须用 `#` 或 `{n}`,不能写死数字。
本地投毒验证过会失败。

### 14.4 顺手发现:8 个 key 我造重了,2 个早就翻译好但没接线

清完之后写了个「哪些 key 没被任何代码引用」的扫描（`scripts/find-unused-i18n-keys.ts`）,
结果 1127 个 key 里有 106 个从没被引用 —— 其中 8 个是**我刚造的 key 的同义词**:

| 我造的                              | 早就存在的                     |
| ----------------------------------- | ------------------------------ |
| `people.builtIn`（内置）            | `admin.label.builtIn`（内置）  |
| `admin.store.readOnlyTitle`（只读） | `admin.label.readOnly`（只读） |
| `admin.store.memberCount`           | `admin.label.memberCount`      |
| `catalog.category.newButton`        | `admin.action.newCategory`     |
| …还有 4 个                          |                                |

已合并到一套,删掉 10 个重复 key。

更值得说的是另外两个:`admin.action.cloneRoles`（克隆角色…）和
`admin.action.transfer`（调动…）**四语译文早就在仓库里**,而对应的两个按钮
一直渲染英文 `Clone roles…` / `→ Transfer…`。
和 §10.8 里 `admin.label.you` / `.noTelegram` / `.neverSeen` 完全同一个模式 ——
**翻译好了,没接线。** 现在接上了。

剩下 94 个未引用 key 是历史遗留（`nav.deliver`、`app.theme.*`、`confirm.empty.noActive` 等）。
**没有批量删** —— 通过计算前缀访问的 key（`i18n.t('unit.' + canonical)`）在源码里
没有字面出现,扫描认识当前在用的 5 个前缀,但漏一个就会误报 90 个 key。
所以它是**人工 triage 工具,不是 CI 门禁**,脚本注释里写清楚了这一点。

### 14.5 这一轮的产出

**清掉的锚定字符串:77 → 0**,守卫从 ratchet 改成**硬门禁**（投毒验证过会失败）。
覆盖 Shell 1 + StoresSections 6 + OperationsSections 28 + PeopleSections 42,
外加各自旁边的正文段落、以及扫描抓不到的:

- 行首带装饰的按钮（`+ New role` / `+ New category` / `+ New supplier`）——
  规则 1 原本要求整行以字母开头,这三个是当时屏幕上最显眼的英文。已放宽规则。
- 英文复数后缀构造 `{n === 1 ? '' : 's'}`（13 处）—— 按构造就不可翻译,
  ru 要三种形式而 zh/uz 不需要,全部改成 ICU 复数。
- 拼接式 toast 的各个片段（`Removed from store` / `Transferred` / `Cloned N…`）。
- `run.status.*` 枚举**一个 key 都没有**（`order.status.*` 有）—— 采购单状态在
  维护页直接渲染 `approved` / `planned` 这种裸枚举。补齐 6 个状态。
- `formatRelative()` 返回硬编码英文（`just now` / `3m ago`）—— 改成接 i18n 实例的纯函数。

**词条量**:1115 → 1132 × 4 语,四语始终对齐（每一步都验证）。

**验证**:11 个包 `tsc` 干净;domain 83 / **i18n 14（新增）** / web 197 / api 86 全绿;
两个守卫本地投毒验证过;五个 tab 全屏扫描,残留的拉丁字符只有 DB 里的乌兹别克语商品名
（`krem balzamik`、`Zaytun yashili` —— 是数据不是文案,正确保留）;无控制台报错。

### 14.6 我在这一轮做的一个判断

花在「修检测器」上的时间明显超过了「修字符串」。第五条规则改了四版还不收敛的时候,
我停了下来把它删掉 —— 而不是继续调正则。

理由:检测器是手段,不是目标。它已经给出了最有价值的东西（那条「标题旁边有正文」的洞见),
而继续追求「自动枚举全部散文」需要真正的 AST 解析,那是另一个量级的工作,
收益只是省掉我逐文件读一遍代码 —— 而逐文件读代码这件事我本来就要做,
因为正文要不要合并成一个 key、`<strong>` 怎么处理,都得看上下文才能决定。

---

## 15. 你拍板的两条：一条无事可做，一条真有活（第五轮）

### 15.1 舍入策略：没有需要决定的东西

先去量，不去猜（前两次就是猜的，见更正后的 §1.9）：

1. **代码里没有除法。** `run/events.ts:291` 的契约是 `unitPrice * qty = total`，
   一次性总价走 qty=1、单价=总价。全仓 grep「总价 ÷ 数量」零命中。
   `settlement.ts` 每一步都是乘法求和。
2. **分账不变量跨全部真实数据成立**：1,740 条已购行，全部有门店分账，
   `sum(split.qty) = item.purchased_qty` 零不匹配，最大残差 `0.000`。
3. 那条 `23333.30` 是 finished 单里**有人手填的单价**，`3 × 23333.30 = 69,999.90`
   是精确乘法。

**所以没有舍入损失，也没有舍入策略要定。** §1.9 已彻底更正，从待决策项移除。

我对这同一个 `.9` 连错两次（先说浮点残留、纠正后说除法丢钱），两次都是从一个数字
往回推机制而没先查数据来源。这一条记在这里。

### 15.2 一天多批：`BatchStrip` 写了但从没渲染过 🔴

你说要给用户看见 —— 查下去发现**只做了一半**：

- ✅ `order.todayBatches` 端点存在
- ✅ 粘性条显示 `今日 2 批`
- ❌ **`BatchStrip` 组件定义完整、带完整 docblock、`grep '<BatchStrip'` 零命中**

那个计数是个**死数字**：它告诉你「还有别的批次」，然后不给你任何看的办法。
docblock 自己写着「This is the trace: ... 『我早上那单发出去了吗』不用离开页面就能回答」——
而它从没被挂到树上。

和这次走查里反复出现的是同一个模式：`admin.action.cloneRoles` 译文躺了几个月按钮还是英文；
`ops_wire.py` 崩在写文件之前所以 20 处替换全丢；`AGING_DAYS` 定义好了才被接上。
**「写完了」和「接上了」是两件事。**

**已修**：

1. 只读小票视图里内联渲染 `BatchStrip`（在小票正文上方）。
2. 粘性条的计数从 `<span>` 改成按钮，点开一个 sheet 渲染同一个 strip ——
   **起草状态下也能看**，这才是「再报一批」不至于掉进一个和当天第一单
   长得一模一样的空目录的关键。
3. 加了 `showHeading` prop：sheet 自带标题，strip 在里面就不再重复一遍
   「今日 N 批」（第一版两处都渲染，实测截图里标题出现了两次）。

**实测**（浏览器）：点 `今日 2 批` →
`第 1 批 已提交 · 等待审核 · 1 项` / `第 2 批 草稿 · 正在查看 · 1 项`。

### 15.3 顺带确认

§4.6 的价格陈旧度可见性（初版说「34 天前」只存在于 `sr-only`）**已经修好了**：
`AGING_DAYS` / `GUESS_DAYS` 三档在 `RunViews.tsx:1569-1579` 被消费，
`moneyInk` 让 30 天以上的行成为整行唯一的状态色。那段 docblock 里
「一个每行都穿的颜色不是警告，是墙纸」的推理值得留着。

### 15.4 验证

11 个包 `tsc` 干净；domain 83 / i18n 14 / web 197 / api 86 全绿；
i18n 四语对齐；两个 i18n 守卫 clean；批次 sheet 浏览器实测。
